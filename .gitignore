# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
node_modules.nosync
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# raw reference material (source font deliverables, screen recordings,
# generated image drafts) — not used by the app; the assets actually
# consumed by the site live under public/fonts and public/images
/fontes/
/compatison/
/*.mov
/Generated image*.png
/d9f4dfab-*.png
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# /fly capture — scraped reference material from the external site being
# rebuilt as the /fly section, plus the temporary capture script. Neither
# is part of the app; refs/ and docs/ get committed selectively later once
# the actual /fly section is built for real.
/refs/
/scripts/capture-fly.js

